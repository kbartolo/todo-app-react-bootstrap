# React Bootstrap App to filter a list of services

This is a simple React app with typescript to filter a list of services,
leveraging the bootstrap framework to improve component styling and reduce
development process.
